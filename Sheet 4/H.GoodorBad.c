#include<stdio.h>
#include<string.h>
int main()
{
    char a[100010];
    int i, j, k, n, l, f, w;

    scanf("%d", &n);

    for(w = 1; w <= n; w++){

    scanf("%s",a);
    l = strlen(a);
    l = l-1;

    for(i = 0; i <= l; i++){
        for(j = 0; j <= l; j++){
            f = 0;
            for(k = i; k <= j; k++)
            {

            }
        }
    }
    }
    return 0;

}
